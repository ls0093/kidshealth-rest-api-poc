package getRequest;

import org.testng.annotations.Test;

public class testAPIFullTest {

	@Test
	public void testEmpty() {
		// Response resp = RestAssured.given().header("Accept",
		// "text/xml,application/xml,application/xhtml+xml")
		// .body("kharticles.kharticle.article_title[0]")
		// .body(FixtureHelpers.fixture("request/request.json")).post("/");
//		resp.then().statusCode(200).body("random.object", CoreMatchers.equalTo("value"));
	}
}
